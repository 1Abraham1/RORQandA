<% provide :page_title, t('.title') %>
<% currently_at t('menu.all') %>

<h1 class="mb-4"><%= t('.title') %></h1>

<%= form_tag(all_path, method: "get") do %>
    <div class="col-sm-10 mb-2">
      <%= text_field_tag :search, params[:search],
      placeholder: "Search for title", class: 'form-control' %>
    </div>

  <%= submit_tag "Search", class: 'btn btn-primary btn-lg' %>
<% end %>
<%# <%= link_to t('.new'), new_question_path, class: 'btn btn-primary btn-lg' % > %>
<%= paginate @questions %>

<%= render @questions %>

<%= paginate @questions %>